{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline surface shared by every platform: our own carrier commands plus the core APIs the shell needs.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default", "notification:default"]
}
