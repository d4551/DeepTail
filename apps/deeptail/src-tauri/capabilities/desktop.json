{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only grants. Pairing here is a pasted host URL rather than a camera scan, so no scanner permission is requested.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["core:window:allow-set-title"]
}
