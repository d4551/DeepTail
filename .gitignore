node_modules/
lib/
dist/
*.tsbuildinfo

# Rust / Tauri build output and generated mobile projects
apps/deeptail/src-tauri/target/
apps/deeptail/src-tauri/gen/

.DS_Store
.env
.env.*
!.env.example
.tmp/
# Local bun cache redirected into the workspace when the default cache
# directory is not writable; never shipped, never scanned.
.tmp-bun/


# Mutation testing: the sandbox backup and the run reports.
.stryker-tmp/
reports/
