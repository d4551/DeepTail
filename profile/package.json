{
  "name": "dsh-profile-deeptail",
  "private": true,
  "type": "module",
  "description": "The dsh profile DeepTail installs: the web surface plus the fleet orchestrator and the device-credential tier.",
  "dependencies": {
    "@deeptail/host-fleet": "0.1.0"
  },
  "dsh": {
    "profile": {
      "bundles": [
        "@deepseek-ai/dsh-base",
        "@deepseek-ai/dsh-web-app"
      ],
      "patchReload": "live"
    }
  }
}
